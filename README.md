# todo-prompt
the prompt for creating the simple todo apps using local models

See [PROMPT.md](PROMPT.md) — a build prompt for a Blazor Web App / EF Core / SQLite todo app on .NET 10, written to be handed to a local coding model.
